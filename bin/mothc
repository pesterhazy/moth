#!/usr/bin/env bash
set -euo pipefail && cd "$(dirname "${BASH_SOURCE[0]}")/.."

rm -rf .moth

makezip
moth put --input-file srv/main.zip --repository file:$PWD/repo

# pidkill 8080
# http-server repo &
# sleep 0.5

bin/mothg "$@"
