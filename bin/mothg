#!/usr/bin/env python

import os, sys

def bootstrap():
  pass

SELF_DIR = os.path.dirname(__file__)
ZIP_PATH = os.path.join(SELF_DIR, "..", "out.zip")

if os.path.exists(ZIP_PATH):
  sys.path.insert(0, ZIP_PATH)

try:
  import foo
except ImportError:
  bootstrap()

  import foo

foo.bar()


